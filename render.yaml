services:
  - type: web
    name: asr-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install --production=false
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
      - key: MONGODB_URI
        sync: false
      - key: FRONTEND_URL
        sync: false
